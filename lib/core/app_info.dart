/// Application version string rendered in the splash footer.
///
/// Single source of truth for the human-readable version. Swap for
/// `package_info_plus` in E15 if dynamic build numbers are needed.
const String kAppVersion = 'v1.0';

/// Studio name rendered next to [kAppVersion] in the splash footer.
const String kStudioName = 'am2 studios';
